import {createIconSetFromFontello} from 'react-native-vector-icons';
import fontelloConfig from './config.json'

let IconSet = createIconSetFromFontello(fontelloConfig, "fontello");

export default IconSet